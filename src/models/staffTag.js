export class StaffTag {
  constructor({ id, title, slug, color, bg_color }) {
    this.id = id;
    this.title = title;
    this.slug = slug;
    this.color = color;
    this.bg_color = bg_color;
  }
}
